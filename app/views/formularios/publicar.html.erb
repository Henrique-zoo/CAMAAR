<h1>Publicar Formulário</h1>

<% if flash[:notice] %>
  <p><%= flash[:notice] %></p>
<% end %>

<% if flash[:alert] %>
  <p><%= flash[:alert] %></p>
<% end %>

<div>
  <h2>Resumo</h2>
  <p><strong>Template:</strong> <%= @template.titulo %></p>
  <p><strong>Turmas:</strong></p>
  <ul>
    <% @turmas.each do |turma| %>
      <li><%= turma.nome_exibicao %></li>
    <% end %>
  </ul>
</div>

<%= form_with url: formularios_path do |form| %>
  <div>
    <p>Público-alvo</p>
    <% Formulario.publico_alvos.each do |valor, _| %>
      <div>
        <label>
          <%= radio_button_tag :publico_alvo, valor, false, id: "publico_alvo_#{valor}" %>
          <%= valor == "docentes" ? "Docentes" : "Discentes" %>
        </label>
      </div>
    <% end %>
  </div>

  <%= form.submit "Confirmar Publicação" %>
<% end %>
